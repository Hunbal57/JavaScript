// var c = 300
let a = 300

if(true){
    let a = 10
    const b = 20
    // console.log(a)
 }


// console.log(a)
// console.log(b)
// console.log(c) 

// for (let i = 0; i < array.length; i++) {
//     const element = array[index];
// }

// Nested scope
// Child function can access the variables of parent function

function one(){
    const username = "hitesh"

    function two(){
        const website = "youtube"
        console.log(username)
    }

    // console.log(website)   // not accessible here

    two();
}

// one();


if(true) {
    const username = "hitesh"
    if (username === "hitesh") {
        const website = " youtube"
        // console.log(username + website)
    }
    // console.log(website)
}
// console.log(username)


// ****************Interesting**********************//

// Hoisting in Javascript
// we are accessing the function before declaration

console.log(addOne(5));
function addOne (num) {
    return num + 1
}

// Function/Expression => variable holding a function

addTwo(5)   // error in this case, we cant access the function before declaration in this case
const addTwo = function(num){
    return num + 2
}
